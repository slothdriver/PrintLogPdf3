<Window x:Class="PrintLogPdf3.LoginWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Title="Login"
       Width="400"
       Height="280"
       WindowStartupLocation="CenterScreen"
       ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ID -->
        <StackPanel Grid.Row="0" Margin="0,10">
            <TextBlock Text="ID" FontSize="14"/>
            <TextBox x:Name="IdBox" Height="30" GotFocus="OnInputGotFocus"/>
        </StackPanel>

        <!-- PW -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Password" FontSize="14"/>
            <PasswordBox x:Name="PwBox" Height="30" GotFocus="OnInputGotFocus"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20">
            <Button Content="계정 생성"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="OnCreateAccount"/>
            <Button Content="로그인"
                    Width="100"
                    Click="OnLogin"/>
        </StackPanel>
    </Grid>
</Window>
