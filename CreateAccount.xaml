<Window x:Class="PrintLogPdf3.CreateAccount"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Account"
        Width="420"
        Height="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ID -->
        <StackPanel Grid.Row="0" Margin="0,10">
            <TextBlock Text="ID" FontSize="14"/>
            <TextBox x:Name="IdBox" Height="30"/>
        </StackPanel>

        <!-- Password -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Password" FontSize="14"/>
            <PasswordBox x:Name="PwBox" Height="30"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20">
            <Button Content="취소"
                    Width="80"
                    Margin="0,0,10,0"
                    Click="OnCancel"/>
            <Button Content="생성"
                    Width="80"
                    Click="OnCreate"/>
        </StackPanel>
    </Grid>
</Window>
